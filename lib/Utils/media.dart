import 'package:flutter/material.dart';

class Media{
   static var height = MediaQuery.of(context).size.height;
   static var width = MediaQuery.of(context).size.height;
   
     static BuildContext get context => context;
}